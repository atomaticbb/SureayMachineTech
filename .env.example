# ===========================================
# Database Configuration
# ===========================================
DATABASE_URL="file:./prisma/data/database.db"

# ===========================================
# Resend Email Service Configuration
# ===========================================
# Get API Key from https://resend.com
RESEND_API_KEY=re_xxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com
EMAIL_TO=contact@yourdomain.com
# Optional, local/dev only — comma-separated list that overrides ALL notification
# recipients (inquiry + catalog download), so test emails never hit real inboxes.
# EMAIL_RECIPIENTS_OVERRIDE=test@yourdomain.com

# ===========================================
# Application Configuration
# ===========================================
NODE_ENV=production
PORT=3000
# Allowed frontend domains (comma separated)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===========================================
# Frontend API Configuration (Production)
# ===========================================
# Frontend needs backend API address
# Dev uses /api (same domain), production uses full URL
VITE_API_URL=https://api.yourdomain.com

# ===========================================
# Analytics Configuration (Optional)
# ===========================================
VITE_ANALYTICS_ENDPOINT=/api/analytics
VITE_ANALYTICS_WEBSITE_ID=

